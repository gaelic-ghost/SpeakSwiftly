# Socket Skill Notes

Use this file to collect durable process lessons that should later become
Socket skills, agent guidance, or knowledge-base entries.

## Candidates

- Official-source inventories should be mapped before comparing community ports.
- Speech model porting lanes should pin runtime constants in small fixtures
  before adding runtime code.
- Model-porting research should keep scripts, fixtures, and tests linked from a
  lane README so future agents can restart the work without thread history.
- Public lanes should avoid private model artifacts, local cache paths, and
  machine-specific dependency declarations.
